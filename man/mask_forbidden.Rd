% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{mask_forbidden}
\alias{mask_forbidden}
\title{Apply the \code{forbidden} sentinel to a cost matrix}
\usage{
mask_forbidden(cost_matrix, forbidden = NA)
}
\value{
The cost matrix with sentinel cells replaced by \code{Inf}.
}
\description{
Every front door that documents a \code{forbidden} argument masks with this one,
so \code{lap_solve()}, \code{lap_solve_batch()} and \code{lap_solve_kbest()} read the same
sentinel the same way. NA and Inf cells are forbidden to the solvers
already, so \code{forbidden = NA} is the identity.
}
\keyword{internal}
