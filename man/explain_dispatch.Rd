% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lap_dispatch.R
\name{explain_dispatch}
\alias{explain_dispatch}
\alias{print.dispatch_explanation}
\title{Explain which solver \code{method = "auto"} selects, and why}
\usage{
explain_dispatch(
  cost,
  method = "auto",
  maximize = FALSE,
  cardinality = c("complete", "maximum", "fixed"),
  n_matches = NULL,
  unmatched_penalty = NULL
)

\method{print}{dispatch_explanation}(x, ...)
}
\arguments{
\item{cost}{Numeric matrix, as passed to \code{\link[=assignment]{assignment()}}. \code{NA} or \code{Inf}
entries are treated as forbidden.}

\item{method}{Character scalar. \code{"auto"} (default) reports the automatic
decision. Naming any other method reports that the choice was explicit and
which rule \code{"auto"} would have picked instead.}

\item{maximize, cardinality, n_matches, unmatched_penalty}{As in \code{\link[=assignment]{assignment()}}.
A cardinality other than \code{"complete"} appends dummy columns before the
solver sees the problem, which changes its shape and sparsity, so the rules
are evaluated against the same matrix \code{assignment()} would hand the solver.}

\item{x}{A \code{dispatch_explanation} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{dispatch_explanation}, a list with elements:
\itemize{
\item \code{method} - the solver that will run.
\item \code{explicit} - \code{TRUE} when \code{method} was named rather than dispatched.
\item \code{rule}, \code{condition}, \code{reason} - the rule that fired under \code{"auto"}.
\item \code{considered} - data frame of every rule tested, in order, with
whether it fired.
\item \code{n}, \code{m}, \code{transposed} - problem shape, and whether the solver sees
the transpose. Rectangular problems are transposed internally so the
solver always has at least as many columns as rows.
\item \code{probe} - the single-pass summary the rules read.
}

Invisibly returns \code{x}.
}
\description{
\code{assignment(method = "auto")} picks a solver from a small set of rules
evaluated against one pass over the cost matrix. This reports the decision
without solving: which rule fired, the property that triggered it, the rules
that were tested first and did not fire, and the internal representation the
problem will be handed to the solver in.
}
\details{
The same rule table drives the dispatch itself, so the report cannot drift
from the behaviour.
}
\examples{
explain_dispatch(matrix(runif(400), 20, 20))
explain_dispatch(matrix(sample(0:1, 400, TRUE), 20, 20))

}
\seealso{
\code{\link[=assignment]{assignment()}}
}
