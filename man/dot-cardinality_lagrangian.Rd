% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_cardinality_exact.R
\name{.cardinality_lagrangian}
\alias{.cardinality_lagrangian}
\title{Lagrangian bound for one node}
\usage{
.cardinality_lagrangian(
  problem,
  coefs = NULL,
  lambda = NULL,
  steps = 20L,
  index = NULL,
  edits = NULL,
  incumbent = Inf,
  step0 = NULL,
  tol = 1e-09,
  cost = NULL
)
}
\arguments{
\item{problem}{The node's network, or the pair \code{.balance_flow_problem()}
returns.}

\item{coefs}{Moment coefficients, one per one-sided row.}

\item{lambda}{Starting multipliers, one per row.}

\item{steps}{How many multiplier updates to take.}

\item{index}{The network's index, unless \code{problem} carries one.}

\item{edits}{The node's arc-bound decisions.}

\item{incumbent}{The best objective known, which sets the step scale.}

\item{step0}{An explicit \code{t_0}, overriding that scale.}

\item{tol}{Numeric tolerance for certification and for the row values.}

\item{cost}{Optional distance matrix for the audit.}
}
\value{
A list with \code{bound}, the multipliers that attained it, the relaxed
solve at those multipliers, any moment-feasible solutions the ascent
passed through, and \code{certified}, whether the bound it reports came out of
a certified solve.
}
\description{
Solves the node's network at a sequence of multipliers, each step one
\code{.flow_solve()} and one \code{verify_flow()}, and returns the largest certified
relaxed optimum it reached. Multipliers move along the projected subgradient
with step \code{t_0 / (1 + k)}, warm-started from whatever \code{lambda} is handed in,
which is the parent's best set during a search.
}
\keyword{internal}
