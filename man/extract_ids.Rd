% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_utils.R
\name{extract_ids}
\alias{extract_ids}
\title{Extract and standardize IDs from data frames}
\usage{
extract_ids(df, prefix = "id", id_col = NULL, warn_synthetic = FALSE)
}
\arguments{
\item{df}{Data frame to read ids from.}

\item{prefix}{Prefix for synthesized ids, also the side name used in
messages ("left" / "right").}

\item{id_col}{Name of the id column, or NULL to resolve one.}

\item{warn_synthetic}{If TRUE, warn when ids are synthesized because no id
column was named or found.}
}
\value{
Character vector of IDs.
}
\description{
The id a matching carries is the key every downstream verb joins on, so it
is resolved once, here, and the same resolution answers \code{match_couples()}
and \code{join_matched()}. \code{id_col} names the column to read; with no name given
a column called \code{id} is used, then meaningful row names, then ids
synthesized from \code{prefix}.
}
\details{
Ids read from the data must be unique: a repeated value makes every
id-keyed join downstream many-to-many, which expands rows and attaches one
unit's covariates to another unit's pair. Synthesized ids are unique by
construction and are not checked.
}
\keyword{internal}
