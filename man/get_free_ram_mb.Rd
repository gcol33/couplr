% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_memory.R
\name{get_free_ram_mb}
\alias{get_free_ram_mb}
\title{Estimate available system RAM in megabytes}
\usage{
get_free_ram_mb()
}
\value{
Numeric scalar (MB of available RAM), or \code{NA_real_} if undetermined.
}
\description{
Cross-platform, base-R-only (shells out; no new package dependency).
Never errors: returns \code{NA_real_} if detection fails or the platform is
unrecognized, so callers must treat \code{NA} as "unknown" and fall back to a
fixed threshold rather than skipping the guard entirely.
}
\details{
"Available" means memory an allocation can obtain without swapping, which on
every platform is more than the untouched free list: Linux reports it
directly as \code{MemAvailable}, and on macOS it is the free, inactive and
speculative pages together, since the kernel keeps almost nothing on the
free list and reclaims the rest on demand.
}
\keyword{internal}
