% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_utils.R
\name{design_estimand}
\alias{design_estimand}
\title{What the design identifies}
\usage{
design_estimand(n_left, n_left_matched)
}
\value{
Named list of fields to merge into a result's \code{info}.
}
\description{
Every matching design in the package weights the left side at 1 and the
right side to reproduce the left distribution within a pair, group or
stratum, so what it targets is the effect on the left population: the ATT,
in the orientation the matching layer is written in, where \code{left} holds the
treated units.
}
\details{
\code{focal_discarded} is how many left units the design did not retain.
Calipers, \code{max_distance} and coarsened strata all drop focal units, and the
estimate then speaks about the retained ones rather than about the whole
treated group, which is what \code{\link[=as_matchit]{as_matchit()}} reports when it hands the
estimand to MatchIt and marginaleffects.
}
\keyword{internal}
