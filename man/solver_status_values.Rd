% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lap_status.R
\name{solver_status_values}
\alias{solver_status_values}
\title{Solver status values}
\usage{
solver_status_values()
}
\value{
Character vector of the permitted status values.
}
\description{
The closed set of values a couplr solve result's \code{status} field can take.
}
\details{
\describe{
\item{\code{"optimal"}}{The solver terminated on its own optimality condition
and every row it was asked to match is matched.}
\item{\code{"partial"}}{Feasible, but fewer rows are matched than requested,
under a maximum-cardinality-then-minimum-cost objective.}
\item{\code{"infeasible"}}{No assignment of the requested cardinality exists.}
\item{\code{"eps_optimal"}}{An auction solve terminated at a caller-supplied
epsilon. The result is within \code{n * epsilon} of optimal, which is a bound,
not a certificate.}
\item{\code{"iteration_limit"}}{The solver stopped on an iteration cap rather
than on optimality. The solution is feasible; its optimality is unproven.}
\item{\code{"interrupted"}}{The solver stopped on a time budget or a user
interrupt, part way through. What it had placed respects every capacity,
and is short of what the problem asked it to place, so it is neither an
answer nor evidence that no answer exists.}
\item{\code{"heuristic"}}{Produced by a heuristic. Optimality is neither claimed
nor checked.}
}
}
\examples{
solver_status_values()

}
\seealso{
\code{\link[=verify_assignment]{verify_assignment()}} to turn a status into a checked certificate.
}
