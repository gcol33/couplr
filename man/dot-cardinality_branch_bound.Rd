% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_cardinality_exact.R
\name{.cardinality_branch_bound}
\alias{.cardinality_branch_bound}
\title{Branch and bound over the moment constraints}
\usage{
.cardinality_branch_bound(
  problem,
  index = NULL,
  coefs = NULL,
  dual_steps = 20L,
  branch = c("unit", "pair"),
  node_limit = 500L,
  time_limit = Inf,
  should_stop = NULL,
  cost = NULL,
  tol = 1e-09
)
}
\arguments{
\item{problem}{The network, or the pair \code{.balance_flow_problem()} returns.}

\item{index}{The network's index, unless \code{problem} carries one.}

\item{coefs}{Moment coefficients, one per one-sided row.}

\item{dual_steps}{Multiplier updates per node.}

\item{branch}{Whether to branch on left-unit inclusion or on pairs.}

\item{node_limit, time_limit}{Search budget, in nodes and in seconds.}

\item{should_stop}{Optional predicate of the search state; \code{TRUE} stops the
search the way an interrupt would.}

\item{cost}{Optional distance matrix for the audit.}

\item{tol}{Numeric tolerance for certification, pruning and the row values.}
}
\value{
A list of class \code{cardinality_run}.
}
\description{
Searches the tree of inclusion decisions, bounding every node by its
Lagrangian relaxation and keeping an incumbent that satisfies every stated
constraint. Returns at any interruption with that incumbent and a global
bound that is valid for the whole problem, never with an unproven claim of
optimality.
}
\details{
A single \code{.flow_solve()} is not interruptible, so \code{time_limit} is checked
between nodes and is only as responsive as one solve of the network.
}
\keyword{internal}
