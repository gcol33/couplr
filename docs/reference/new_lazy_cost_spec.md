# Construct a lazy cost specification

Construct a lazy cost specification

## Usage

``` r
new_lazy_cost_spec(left_mat, right_mat, distance, sigma, weights, vars)
```

## Value

An object of class "lazy_cost_spec".
