% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_cardinality_exact.R
\name{.cardinality_solve}
\alias{.cardinality_solve}
\title{Balance-constrained cardinality match}
\usage{
.cardinality_solve(
  left,
  right,
  cost,
  refined = NULL,
  exact = 1L,
  moments = NULL,
  max_std_diff = NULL,
  vars = NULL,
  dual_steps = 20L,
  branch = c("unit", "pair"),
  node_limit = 500L,
  time_limit = Inf,
  should_stop = NULL,
  tol = 1e-09
)
}
\arguments{
\item{left, right}{Data frames of units, one row each.}

\item{cost}{Numeric distance matrix, \code{nrow(left)} by \code{nrow(right)}. A cell
that is \code{Inf} or \code{NA} is a pair the match may not use.}

\item{refined}{The nested groupings balance is required on, coarsest first.}

\item{exact}{How many of the coarsest levels are enforced exactly.}

\item{moments, max_std_diff, vars}{Moment constraints, as \code{.moment_specs()}
takes them.}

\item{dual_steps}{Multiplier updates per node.}

\item{branch}{Whether to branch on left-unit inclusion or on pairs.}

\item{node_limit, time_limit}{Search budget.}

\item{should_stop}{Optional predicate of the search state; \code{TRUE} stops the
search the way an interrupt would.}

\item{tol}{Numeric tolerance for certification and for the constraint values.}
}
\value{
A \code{cardinality_report}.
}
\description{
Builds the balance network for a distance matrix and a nested partition,
searches it under whatever moment constraints are stated, and reports the
matched sample with the bound that goes with it.
}
\keyword{internal}
