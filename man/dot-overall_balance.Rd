% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_diagnostics.R
\name{.overall_balance}
\alias{.overall_balance}
\title{Summarise a per-variable balance table}
\usage{
.overall_balance(var_stats, n_vars)
}
\arguments{
\item{var_stats}{A per-variable balance table, as
\code{calculate_var_balance()} rows bound together.}

\item{n_vars}{How many variables the balance was asked about. This is the
number asked for rather than \code{nrow(var_stats)}, which is smaller when
a variable produced no statistics.}
}
\value{
List with \code{mean_abs_std_diff}, \code{max_abs_std_diff},
\code{pct_large_imbalance} and \code{n_vars}.
}
\description{
The three headline numbers every balance object reports, read off the
per-variable table each of them builds.
}
\keyword{internal}
