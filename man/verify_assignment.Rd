% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lap_certify.R
\name{verify_assignment}
\alias{verify_assignment}
\alias{print.assignment_certificate}
\title{Verify that an assignment is optimal}
\usage{
verify_assignment(x, cost = NULL, duals = NULL, maximize = FALSE, tol = 1e-09)

\method{print}{assignment_certificate}(x, ...)
}
\arguments{
\item{x}{An \code{assignment_certificate} object.}

\item{cost}{Numeric cost matrix the assignment was computed on, or a lazy
cost specification from \code{\link[=compute_distances]{compute_distances()}}. Required unless \code{x} already
carries one.}

\item{duals}{Optional list with elements \code{u} and \code{v} giving row and column
potentials in the orientation of \code{cost}. Overrides any duals on \code{x}.}

\item{maximize}{Logical; whether the assignment maximized rather than
minimized. Defaults to \code{FALSE}.}

\item{tol}{Numeric tolerance for the feasibility and slackness comparisons.
The duality-gap comparison scales this by the magnitude of the objective,
since an absolute tolerance on a sum of many terms is not reachable in
double precision.}

\item{...}{Ignored.}
}
\value{
An object of class \code{assignment_certificate}, a list with elements:
\itemize{
\item \code{certified_optimal} — logical, the conclusion. \code{TRUE} only when
every condition below holds.
\item \code{primal_feasible} — logical; no column claimed twice, no forbidden
pair matched, no index out of range.
\item \code{dual_feasible} — logical; \code{c_ij - u_i - v_j >= -tol} over every
admissible pair, and, when there are more columns than rows,
\code{v_j <= tol} for every column.
\item \code{complementary_slackness} — logical; both halves.
\item \code{cs_matched_tight}, \code{cs_unmatched_free} — the two halves separately.
\item \code{primal_objective}, \code{dual_objective}, \code{duality_gap} — numeric.
\item \code{min_reduced_cost}, \code{worst_i}, \code{worst_j} — the most violated pair, if
any.
\item \code{max_matched_slack}, \code{max_v_unmatched}, \code{max_v} — the quantities the
slackness and sign conditions bound.
\item \code{n_matched}, \code{n_rows}, \code{n_cols}, \code{transposed}, \code{tolerance}.
}

Invisibly returns \code{x}.
}
\description{
Checks a solved assignment against the linear-programming optimality
conditions and returns the result of each check. Unlike the \code{status} field on
a solve result, which records what the solver terminated on, this is a proof:
\code{certified_optimal} is \code{TRUE} only when every condition holds.
}
\details{
The check needs dual variables. If \code{x} carries them (as
\code{\link[=assignment_duals]{assignment_duals()}} results do), they are used. Otherwise they are obtained
by solving \code{cost} with \code{\link[=assignment_duals]{assignment_duals()}}, which costs a second solve.
Either way the duals are verified, not trusted: dual feasibility is checked
over every admissible pair, so duals that do not certify anything cause the
verification to fail rather than pass.

Optimal duals are shared by all optimal solutions of a linear program, so a
matching from one solver can be certified against duals from another. That is
what makes it possible to certify solvers that return no duals of their own.
}
\examples{
set.seed(1)
cost <- matrix(runif(100), 10, 10)
verify_assignment(assignment(cost), cost)

# A rectangular problem, where the condition on unmatched columns bites.
# Passing the duals result reuses its duals instead of solving again.
rect <- matrix(runif(120), 6, 20)
verify_assignment(assignment_duals(rect), rect)

}
\seealso{
\code{\link[=assignment]{assignment()}}, \code{\link[=assignment_duals]{assignment_duals()}}, \code{\link[=solver_status_values]{solver_status_values()}}
}
