% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_memory.R
\name{vm_stat_available_mb}
\alias{vm_stat_available_mb}
\title{Available megabytes implied by a block of \code{vm_stat} output}
\usage{
vm_stat_available_mb(vm, page_size)
}
\arguments{
\item{vm}{Character vector of \code{vm_stat} output lines.}

\item{page_size}{Page size in bytes, as returned by \code{\link[=vm_stat_page_size]{vm_stat_page_size()}}.}
}
\value{
Numeric scalar (MB available), or \code{NA_real_} if unparseable.
}
\description{
Inactive and speculative pages are reclaimed on demand, so they count as
available to an allocation in the same sense as Linux's \code{MemAvailable}; macOS
keeps almost nothing on the free list, so the free count alone understates
what a large matrix can actually obtain.
}
\details{
Split from \code{\link[=get_free_ram_mb]{get_free_ram_mb()}} so the page-size and page-class handling can be
checked without a macOS host.
}
\keyword{internal}
