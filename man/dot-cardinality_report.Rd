% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_cardinality_exact.R
\name{.cardinality_report}
\alias{.cardinality_report}
\alias{print.cardinality_report}
\title{Report of a cardinality match}
\usage{
.cardinality_report(run, specs = NULL, tol = 1e-09)

\method{print}{cardinality_report}(x, ...)
}
\arguments{
\item{run}{A \code{cardinality_run} from \code{\link[=.cardinality_branch_bound]{.cardinality_branch_bound()}}.}

\item{specs}{The moment rows the run was given, from \code{.moment_specs()}.}

\item{tol}{Numeric tolerance for reading a constraint as satisfied.}

\item{x}{A \code{cardinality_report}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cardinality_report}, a list with elements:
\itemize{
\item \code{n_matched}, \code{n_left_matched} - matched pairs, and the left units
they use.
\item \code{best_possible} - the largest matched sample the bound admits.
\item \code{gap}, \code{gap_fraction} - \code{best_possible - n_matched}, in matched
units and as a share of \code{best_possible}.
\item \code{certified} - \code{TRUE} only when the search settled, every solve its
bound rests on was certified, the incumbent's own solve was
certified and audited, and \code{gap} is zero.
\item \code{objective}, \code{bound} - the incumbent's value of the network
objective and the global lower bound on it. \code{best_possible} is read
from \code{bound}.
\item \code{stopped_on}, \code{n_nodes}, \code{engine}, \code{status}.
\item \code{constraints} - one row per stated constraint, with what it asked
for and what the matched sample achieved.
\item \code{balance} - matched counts and imbalance per category at every
level.
\item \code{tiers}, \code{precision_headroom}, \code{shift} - the weights that order the
objective, how many times its range fits inside the range a double
orders exactly, and the constant taken off every distance.
\item \code{total_distance}, \code{pairs} - the matched set itself.
}

Invisibly returns \code{x}.
}
\description{
Turns a completed search into the object callers read: the matched sample's
size, the largest size the bound admits, the gap between them, and the state
of every constraint the match was asked to meet.
}
\keyword{internal}
